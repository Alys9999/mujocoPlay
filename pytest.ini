[pytest]
markers =
    fast: fast pure-python unit tests
    io: tests that write files
    compat: compatibility and wrapper integration tests
    network: tests that require network access
    gpu: tests that require GPU access
